# AI Agent Application Configuration

# === Core Application Settings ===
HOST=0.0.0.0
PORT=8000

# === Provider Selection ===
EMBEDDING_PROVIDER=openai
LLM_PROVIDER=openai

# === LLM Settings ===
LLM_TEMPERATURE=0.7
RETRIEVER_K=3

# === Performance Tuning ===
# Options: similarity, mmr, similarity_score_threshold
RETRIEVER_SEARCH_TYPE=similarity
# Only used if RETRIEVER_SEARCH_TYPE=similarity_score_threshold (0.0-1.0)
RETRIEVER_SCORE_THRESHOLD=0.2
# Max chat turns to keep in context (reduce for faster responses)
CHAT_HISTORY_MAX_TURNS=8
# Batch size for embedding requests (LM Studio, etc.)
EMBEDDING_BATCH_SIZE=64

# === OpenAI Configuration ===
OPENAI_API_KEY=your_openai_api_key_here

# === Ollama Configuration ===
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_EMBEDDING_MODEL=granite-embedding:278m
OLLAMA_LLM_MODEL=llama3.2:3b

# === Azure OpenAI Configuration ===
AZURE_OPENAI_API_KEY=your_azure_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-02-01
AZURE_OPENAI_EMBEDDING_DEPLOYMENT_NAME=text-embedding-ada-002
AZURE_OPENAI_LLM_DEPLOYMENT_NAME=gpt-35-turbo

# === AWS Bedrock Configuration ===
BEDROCK_EMBEDDING_MODEL_ID=amazon.titan-embed-text-v1
BEDROCK_MODEL_ID=anthropic.claude-v2
AWS_REGION=us-east-1
# Option 1: Use AWS Profile
BEDROCK_PROFILE_NAME=your_aws_profile
# Option 2: Use AWS Access Keys (if not using profile)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# === LM Studio Configuration ===
LM_STUDIO_BASE_URL=http://localhost:1234
LM_STUDIO_API_KEY=lm-studio
LM_STUDIO_EMBEDDING_MODEL=text-embedding-qwen3-embedding-0.6b
LM_STUDIO_MODEL=qwen2.5-coder-7b-instruct


# OpenRouter Configuration
OPENROUTER_API_KEY=your_openrouter_api_key_here
OPENROUTER_MODEL=anthropic/claude-3.5-sonnet
# Popular OpenRouter LLM models:
# - anthropic/claude-3.5-sonnet (recommended)
# - openai/gpt-4o
# - meta-llama/llama-3.1-8b-instruct
# - google/gemini-pro-1.5
# - anthropic/claude-3-haiku (faster, cheaper)
# See https://openrouter.ai/models for full list

OPENROUTER_EMBEDDING_MODEL=text-embedding-3-small
# Popular OpenRouter embedding models:
# - text-embedding-3-small (OpenAI, recommended)
# - text-embedding-3-large (OpenAI, higher quality)
# - text-embedding-ada-002 (OpenAI, legacy)